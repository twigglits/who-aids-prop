#ifndef PARALLEL_H

#define PARALLEL_H

// Used to disable the #pragma omp stuff, to disable OpenMP based parallellization, but
// still using locks and other code changes that will cause some extra effort
//#define DISABLE_PARALLEL

#endif // PARALLEL_H
